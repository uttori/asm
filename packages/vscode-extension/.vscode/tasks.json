{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Build SNES Assembly Extension",
      "type": "shell",
      "command": "npm run lsp:build && npm run vscode:build",
      "options": {
        "cwd": "${workspaceFolder}/../.."
      },
      "problemMatcher": ["$tsc"],
      "group": "build"
    }
  ]
}

# TODO

Goal: Match log output

- Follow entry in `asar/src/asar/interface-cli.cpp` and port to JS in interface.js
- Logic is all over the place, many shared variables between various classes (example `pass` is assumed to exist everywhere)

---

asar_math.h and macro.h
– These are a bit more advanced but are still supporting libraries that the main assembler logic depends on.
